# simple_mcp

A minimal **Model Context Protocol (MCP)** server in TypeScript — a small,
clean starting point for building your own MCP tools.

## Stack
TypeScript · MCP

## Run
```bash
pnpm install
pnpm start
```
